# dash-hover-hold
GNOME extension that prevents the Dash and Overview from closing automatically when clicking on an application. Useful for opening up multiple applications at once while preserving the native Gnome Dash.
